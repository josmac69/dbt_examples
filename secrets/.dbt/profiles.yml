data_quality:
  outputs:

    dev:
      type: postgres
      threads: 1
      host: postgres
      port: 5432
      user: myuser
      pass: mypassword
      dbname: main
      schema: public
  logs:
    log-level: debug
  log-level: debug

  target: dev

incremental_models:
  outputs:

    dev:
      type: postgres
      threads: 1
      host: postgres
      port: 5432
      user: myuser
      pass: mypassword
      dbname: main
      schema: public
  logs:
    log-level: debug
  log-level: debug

  target: dev
